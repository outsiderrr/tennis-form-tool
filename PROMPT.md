# 在其他对话里接手这个项目 · 可直接粘贴的提示词

复制下面整段发给任何 Claude / 其他 AI 会话即可。

---

我在做一个网球动作可视化反馈工具，仓库：https://github.com/outsiderrr/tennis-form-tool（私有仓库；如果你能访问本机，代码在 `~/通用空间/tennis-form-tool/`）。

请先完整阅读仓库里的 `CONTEXT.md`（项目简报：为什么这么做、已定前提、当前状态、指标口径、训练与拍摄约定、已知失败模式、下一步），再读 `README.md`（怎么运行）。**CONTEXT.md 里「已定的前提」不要重新讨论**，直接在此基础上工作。

关于我：网球初学者，右手持拍，看视频自学，和朋友休闲对拉；健康安全对我是高优先项，给动作建议时默认保守、不鼓励极限发力，但不要特殊化强调某个具体身体部位。我认同的练法：自下而上、每球前静态摆好几个维度、挥拍时只想一个、达标率 70–80% 就加难度、口令用外部焦点。

工具的原则：反馈要**可视、可验证、发生在我自己身体上**——先给测量值再给解释，不要用 LLM 生成教练话术代替测量；所有数字看趋势不抠个位数（阈值是原理占位口径）。

环境：macOS，Python 3.12 虚拟环境在仓库的 `.venv/`（`mediapipe<1.0`，1.0.x 在 macOS 会崩），模型文件 `models/pose_landmarker_full.task` 需按 README 下载。每周用法是一条命令：`.venv/bin/python session.py <视频.MOV> --player me --segments "0-180:定点,180-360:变化" --label "日期 机位"`，结果在 `sessions/<player>/`。

现在的任务是：【在这里写你这次要做的事，例如：分析我新拍的视频 / 补侧面机位的上半身指标 / 改进关键时刻卡 / 讨论下一阶段】

---

## 按场景的补充句（按需追加到上面）

- **只是分析新视频**：「素材在 `<路径>`，背面/侧面机位，第 X 秒到 Y 秒是定点、之后是变化。跑 session.py，然后按 CONTEXT.md 的口径给我进度表解读和下一步训练重点。」
- **补上半身指标**（有侧面素材后）：「素材是侧面机位。请在 report.py 里新增：转肩幅度、击球点相对髋的前后距离、躯干前倾角 vs 屈膝角（弯腰还是弯膝，安全项），并接进 card.py 第②格。先在素材上验证再改卡。」
- **推进 B 阶讨论**：「先读 CONTEXT.md 的 P4 和 B 阶进入条件，评估现在是否满足，不满足就说清楚缺什么。」
- **给球友出报告**：「视频里可能有多人，目标是离镜头最近的那位；输出关键时刻卡 + 分维度建议，做得好的先肯定，要抬高的按优先级给一件事、一句外部焦点口令。」
